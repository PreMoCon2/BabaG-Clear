<?xml version="1.0" encoding="utf-8"?>
<!--
     The service listens to window changes because Recents, launcher, lockscreen,
     and notification shade transitions are all reported through accessibility
     window events rather than through a dedicated Recents API.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowsChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows|flagIncludeNotImportantViews"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="80"
    android:settingsActivity="com.example.autoclear.MainActivity" />
